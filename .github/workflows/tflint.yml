name: tf-lint
on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:
jobs:
  tf-lint:
    uses: ruchit-cd/github-shared-workflows/.github/workflows/tf-lint.yml@fix/readme
    secrets:
      GITHUB: ${{ secrets.GITHUB }}
