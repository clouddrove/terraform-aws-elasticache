name: tf-lint
on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:
jobs:
  tflint:
    uses: clouddrove/test-tfsec/.github/workflows/tf-lint.yml@1.0.9
    secrets:
      GITHUB: ${{ secrets.GITHUB }}
